import type { ColorPack } from '../types/siteThemes';
export declare const colorGroups: Record<string, ColorPack[]>;
declare const colors: ColorPack[];
export declare const getColorPack: (theme: string, index: number) => ColorPack;
export default colors;
